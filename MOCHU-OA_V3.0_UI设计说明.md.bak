# MOCHU-OA V3.0 UI 设计说明文档

## 1. 设计目标

1. 提供清晰、可用的业务管理界面，支撑：
   - 收入对账管理
   - 回款督办管理
   - 付款计划管理
   - 日常报销管理
   - 成本归集与发票管理
   - 采购清单与价格匹配
   - 询价与比价分析
   - 供应商评级与价格水平管理
   - 物资出库与跨项目调拨管控
   - 甘特图与收入拆分明细联动
2. 基于统一视觉和交互规范，保证系统一致性、易用性与操作效率。
3. 支持权限细分、角色可见范围控制，并保持界面仅显示可操作内容。
4. 强化数据展示、异常提醒与导出能力，满足财务人员对账、监督、审阅和决策需求。

---

## 2. 设计原则

- **清晰结构**：主导航按模块分层，功能点明显；页面布局遵循信息优先、纵向拆分。
- **角色适配**：不同角色显示不同操作按钮；审批角色只展示审批相关动作。
- **操作防错**：关键动作加二次确认，输入校验即时反馈。
- **数据可视化**：重点金额、差异、完成率以图表/标签突出。
- **统一风格**：表格、表单、弹窗、卡片统一样式，符合企业级管理系统。
- **响应适配**：适配常规1280+分辨率，关键页面支持平板或小屏浏览。

---

## 3. 页面结构概览

### 3.1 主导航

主导航包含以下一级菜单：
- 首页
- 项目管理
- 合同管理
- 供应商管理
- 采购管理
- 物资管理
- 施工管理
- 财务管理
  - 收入对账
  - 回款督办
  - 付款计划
  - 日常报销
  - 发票管理
- 报表中心
- 审批中心
- 系统设置

### 3.2 二级菜单

财务管理下的二级菜单：
- 收入对账
- 回款督办
- 付款计划
- 日常报销
- 发票管理
- 成本归集

### 3.3 首页设计

#### 3.3.1 首页定位

首页是系统入口首页，主要展示用户常用入口、最新待办、系统公告、最近访问和个性化功能入口。首页设计应满足：
- 快速进入核心业务功能
- 最近待办信息立即可见
- 最新公告及时展示
- 不同角色显示不同入口
- 用户可定制个性化入口

#### 3.3.2 首页结构

首页主要由以下区域组成：
- 顶部导航与欢迎区
- 左侧可折叠导航栏
- 主体主内容区：
  - 常用功能入口
  - 当前待办（最近 10 条）
  - 最新公告（最近 5 条）
  - 最近访问
  - 其他快捷入口

#### 3.3.3 顶部导航与欢迎区

- 顶部包含：Logo、系统标题、欢迎语、用户头像+下拉菜单
- 右侧显示当前用户角色与快捷消息提醒
- 搜索入口可快速定位项目/合同/待办

#### 3.3.4 左侧导航栏

- 可折叠的权限动态导航列表
- 仅显示当前用户有权限访问的模块
- 支持快捷折叠/展开
- 顶部显示用户当前登录部门/角色

#### 3.3.5 常用功能入口

- 包含三个层级入口：
  - 系统级默认入口：项目列表、通讯录、系统公告
  - 角色级默认入口：根据角色动态展示事务入口
  - 用户级个性化入口：用户可添加/删除/排序，最多 12 个
- 每个入口以卡片形式展示，支持图标+名称+简短提示
- 点击入口直接跳转对应模块或页面

#### 3.3.6 当前待办区

- 显示当前用户的最近 10 条待办事项
- 支持“查看全部”跳转到完整待办列表
- 每条待办展示：标题、业务类型、关联项目、当前节点、时间、优先级
- 仅显示最新10条，避免首页信息过载
- 待办区支持5分钟轮询刷新

#### 3.3.7 系统公告区

- 展示最新 5 条公告，按发布时间倒序
- 置顶公告始终排在最前
- 过期公告自动隐藏
- 公告项支持“查看详情”按钮

##### 3.3.7.1 公告图片轮播

- 轮播区域展示已发布且带封面图的公告
- 默认显示最新 5 条轮播公告
- 支持自动轮播、手动切换、暂停/继续
- 提示点显示当前轮播位置
- 轮播图支持点击跳转至公告详情
- 轮播图片加载失败时显示默认空态图
- 轮播组件失败不影响首页其他模块正常加载

##### 3.3.7.2 公告轮播内容规则

- 仅展示“已发布”状态且`cover_image`存在的公告
- 置顶公告优先进入轮播
- 轮播数据缓存 5 分钟，公告发布/下线时刷新缓存
- 若轮播数量少于 3 条，自动关闭轮播动画，改用静态公告卡片展示

#### 3.3.8 最近访问区

- 展示用户最近访问的模块/页面
- 支持点击快速跳转
- 最近访问项按时间倒序展示

#### 3.3.9 其他快捷入口区域

- 提供额外快捷操作入口，如：
  - 新建对账单
  - 生成督办计划
  - 新建报销单
  - 查询发票认证
  - 查看月度付款计划
- 入口可与个性化入口区合并，保持首页简洁

#### 3.3.10 首页交互细节

- 页面头部、左侧导航与主体区应分层展示
- 首页数据默认每 5 分钟刷新一次，支持手动刷新按钮
- 公告轮播载入失败或无数据时显示空态提示
- 待办/公告模块失败不影响整体首页加载
- 用户级入口支持拖拽排序
- 首页入口按钮根据角色显示，非权限用户隐藏无效入口

---

## 4. 页面的通用组件规范

### 4.1 页面头部

- 页面标题 + 当前功能模块
- 面包屑：系统 / 财务管理 / 当前页面
- 操作按钮区：新增、导出、刷新、帮助
- 统计摘要卡片：关键数据（如待审批数量、总金额、预警数）

### 4.2 筛选区

- 位置：列表页顶部或表格上方
- 组件：搜索框、下拉筛选、日期区间选择、项目/合同常用标签
- 交互：筛选后自动刷新列表，支持“重置”按钮

### 4.3 数据表格

- 列展示要点：编号、项目、合同、金额、状态、审批状态、操作
- 单行操作：查看/编辑/提交/审批/导出
- 状态标签：正常、预警、逾期、已完成、已驳回，采用色彩区分
- 列头排序：金额、时间、优先级支持排序
- 分页：每页20条，支持快速跳转

### 4.4 表单结构

- 卡片式分组：基础信息、明细信息、附件、审批信息
- 必填字段用红色星号标注
- 动态表单：根据报销类型、合同绑定显示不同字段
- 金额字段：添加千位分隔、实时校验
- 附件上传：拖拽/点击上传，显示上传进度与状态

### 4.5 弹窗与提示

- 确认弹窗：关键操作（提交、审批、删除、导出）
- 提示方式：Toast提示 + 弹窗确认
- 表单错误提示：字段下方即时提示
- 预警提示：黄色/红色banner或浮层提示

### 4.6 审批轨迹

- 时间线展现审批节点、审批人、审批时间与意见
- 驳回原因高亮展示
- 审批备注支持展开查看

---

## 5. 关键模块 UI 设计

### 5.1 收入对账页

#### 5.1.1 列表页面

- 左侧：筛选区域
  - 项目、合同、状态、对账周期
  - 日期范围选择
- 右上：关键统计卡片
  - 待确认对账单数、异常对账单数、当月金额总计
- 主表格：
  - 对账单编号、项目名称、所属期间、合同金额、当期回款、应收余额、审批状态、操作
  - 状态标签：已提交、审批中、已完成、异常
- 行操作：查看、编辑、提交、导出

#### 5.1.2 对账单详情页

- 基础信息卡片：对账单编号、项目、合同、期间、状态
- 金额概览模块：
  - 合同含税金额、当期产值、累计产值、当期回款、累计回款、应收余额
- 附件模块：
  - 文件列表、预览/下载/删除
- 差异分析卡片：
  - 上期金额、本期金额、差异金额、差异率
- 差异明细表：
  - 左列上期、右列本期、中列差异
  - 仅差异/全部切换
- 审批区：
  - 审批进度、审批记录、操作按钮

#### 5.1.3 差异分析页

- 期次选择器：上期/本期可选
- 对比列表：
  - 客户/合同/金额、差异、原因备注
  - 差异行高亮显示
- 图表区域：
  - 柱状图显示上期 vs 本期
  - 折线图显示趋势
- 导出按钮：Excel/PDF

### 5.2 回款督办页

#### 5.2.1 督办计划列表

- 筛选项：项目、合同、优先级、状态、超期等级
- 统计卡：督办中数量、逾期数量、总缺口金额
- 表格字段：
  - 计划编号、项目、合同、节点、回款缺口、完成率、逾期天数、优先级、审批状态、督办状态
- 重点颜色：
  - 逾期红、预警橙、正常绿
- 行操作：查看、审批、转办、导出

#### 5.2.2 计划详情页

- 基本信息区：
  - 项目信息、合同信息、计划编号、责任人
- 校验依据区：
  - 合同条款摘要、甘特图节点、竣工资料状态
- 回款对比区：
  - 应付金额、已回金额、缺口、完成率
  - 图表：蓝/橙柱状对比
- 督办建议区：
  - 优先级、建议时限、行动建议
- 执行反馈区：
  - 反馈列表、附件、预计回款时间、问题说明
- 审批区：
  - 审批记录时间线、操作按钮（同意/驳回）

### 5.3 采购清单管理页

#### 5.3.1 采购清单列表页

- 页面顶部操作区：
  - 按钮：新建采购清单、从合同附件生成草稿、刷新
  - 仅预算员可见“新建采购清单”和“从合同附件生成草稿”按钮
- 筛选区：
  - 项目、合同编号、合同名称、清单状态（草稿/待审批/已审批/已完成）、创建时间、来源类型（附件导入/手工创建）
- 列表字段：
  - 采购清单编号、关联合同编号、关联项目、清单状态、来源、预算总额、创建人、创建时间、操作
- 行操作：
  - 查看、编辑（草稿可编辑）、提交审批、删除
- 来源标识：
  - 附件导入生成的草稿显示“附件导入”标签；手工新建显示“手工创建”标签
- 合同附件按钮：
  - 合同详情页及采购清单列表页支持“从合同附件生成草稿”入口
  - 点击按钮后进入附件解析结果页，展示导入的物资条目和解析状态

#### 5.3.2 采购清单草稿编辑页

- 页面结构：
  - 基础信息区：合同信息、关联项目、清单来源、清单状态、编制人、编制时间
  - 明细信息区：采购清单物资明细表
  - 操作区：保存草稿、提交审批、取消
- 物资明细表：
  - 字段：物资名称、规格型号、品牌、单位、计划数量、预算单价、预算合价、备注
  - 行操作：新增行、批量新增、复制行、删除行
  - 批量写入：
    - 提供“批量写入”按钮，弹出批量输入框
    - 支持按行粘贴多条物资信息（如：物资名称/规格型号/品牌/单位/计划数量）
    - 系统对粘贴数据进行智能解析，缺失字段留空以便补充
  - 倒表操作：
    - 明细表右上显示“倒序/正常顺序”切换按钮
    - 一键切换后表格顺序翻转，用于快速倒表校对和匹配合同附件顺序
- 交互规则：
  - 草稿状态可自由编辑，字段根据合同类型动态展示
  - 设备类物资品牌为必填项，不填写时禁止保存/提交并提示“设备类物资必须填写品牌”
  - 预算单价自动代入规则保持一致，若无基准价则行高亮提示
  - 提交审批前校验：关联合同、计划数量、必填字段、品牌规则、附件导入项确认
- 草稿提示：
  - 草稿顶部显示“当前为采购清单草稿，可修改后提交审批”
  - 若来源为合同附件，显示“来源：合同附件导入”并列出原合同附件名称

##### 5.3.2.1 草稿明细编辑区

- 明细表头增加“序号/物资名称/规格型号/品牌/单位/计划数量/预算单价/预算合价/备注”
- 每行价格字段自动计算，单价不可直接编辑（若有基准价）
- 单行右侧显示“行标签”用于标识“设备类”“材料类”“无基准价”
- 行级校验提示：
  - 计划数量为空或≤0时高亮红色提示
  - 设备类物资品牌未填写时显示 Tooltip “设备类物资必须填写品牌”
  - 无基准价行行尾提示“已按0元记取，待同步基准价”
- 表格底部显示合计行：总计划数量、预算总额、异常行数

##### 5.3.2.2 批量写入与倒表操作

- 批量写入流程：
  - 点击“批量写入”弹出多行输入框，支持“物资名称,规格型号,品牌,单位,计划数量”格式
  - 右侧展示字段说明和示例行
  - 点击“解析并导入”后，系统预览解析结果并标记缺失字段
  - 解析结果页允许一键“补全缺失字段”“全部导入”“取消”
- 倒表操作体验：
  - 右上角显示“顺序/倒序”切换按钮
  - 切换后仅改变视觉顺序，不修改实际行数据
  - 倒序模式下，表头固定，行序号保持对应明细
  - 适用于校对合同附件原始顺序或与文档倒序比对

#### 5.3.3 合同附件导入流程

- 入口位置：
  - 合同详情页附件区显示“导入附件生成采购清单草稿”按钮
  - 采购清单列表页显示“从合同附件生成草稿”入口
- 导入体验：
  - 点击后展示解析进度与预览结果
  - 解析成功后展示待导入物资列表，支持编辑、删除、补充
  - 解析失败或缺失项时展示失败原因，并提供手工补充入口
- 无附件处理：
  - 若合同无附件，则按钮显示灰色提示“当前合同无附件，请直接创建采购清单”
  - 预算员可直接进入“新建采购清单”页面手工创建草稿

##### 5.3.3.1 合同附件导入交互细节

- 点击“导入附件生成采购清单草稿”后，弹出“导入进度”浮层：
  - 解析中：显示进度条与正在解析文件名
  - 成功：展示“解析完成，X 条物资已生成”
  - 失败：展示失败原因并提供“重新上传”“转手工创建”
- 解析结果页：
  - 左侧列表展示所有提取的物资行
  - 右侧显示原合同附件名称、上传时间、解析成功率
  - 物资行支持快速编辑：品牌、单位、数量、备注
  - 缺失项高亮红色，提示“请补全字段后继续”
- 结果确认按钮：
  - “保存草稿”：保存解析结果为采购清单草稿
  - “继续编辑”：进入草稿编辑页完善内容
  - “取消”：放弃解析结果返回采购清单列表
- 导入来源标识：
  - 草稿页面顶部显示“来源：合同附件导入”
  - 草稿详情页显示“原始合同附件：xxx.pdf”链接

#### 5.3.4 手工新建采购清单流程

- 直接创建入口：
  - 采购清单列表页“新建采购清单”按钮
  - 合同详情页若无附件时显示“手工创建采购清单”入口
- 新建页功能：
  - 支持一次性多行写入：
    - 提供“批量写入”按钮，可粘贴多条明细数据
    - 提供“复制粘贴新增行”按钮，减少逐行输入成本
  - 支持倒表操作：
    - 明细表支持“顺序/倒序”切换
    - 允许快速按当前表头排序或反向显示
- 保存方式：
  - 可保存为草稿，后续继续编辑
  - 草稿可在列表页直接编辑并重新提交审批

#### 5.3.5 草稿与审批状态

- 草稿状态说明：
  - 草稿：可编辑、可保存；未进入审批流程
  - 待审批：已提交后只读，展示审批进度
  - 已审批：审批完成，不可编辑，仅可查看
  - 驳回：可修改并重新提交
- 页面提示：
  - 草稿页面顶部显示当前状态、最后保存时间、审批说明
  - 编辑页面右侧显示校验提示，提醒“设备类品牌必填”“计划数量必须大于0”“报价缺失需补充”等
- 操作按钮：
  - 草稿页：保存草稿、提交审批、取消
  - 驳回页：继续编辑、重新提交

##### 5.3.5.1 草稿页面视觉布局

- 页面顶部：
  - 左侧显示“采购清单草稿”标题、清单编号（草稿暂未编号可显示“待生成”）
  - 右侧显示当前状态标签、最后保存时间、来源标签
- 基础信息区：
  - 采用卡片式布局，显示合同信息、关联合同编号、关联项目、编制人、编制时间、来源类型
  - 若来源为合同附件导入，显示“原始附件名称+解析时间”说明行
- 明细表区：
  - 采用全屏表格展示，支持固定列、行悬停高亮
  - 表格上方放置“批量写入”“倒序/正常顺序”“下载模板”“导入附件”按钮
  - 表格底部固定显示合计行和当前异常行数
- 右侧操作信息栏：
  - 显示“当前草稿需完成的校验项”“待补充字段总数”“提示信息”
  - 提示示例：设备类品牌必填、无基准价行需复核、合同附件解析后需确认
- 底部固定操作条：
  - 按钮：取消、保存草稿、提交审批
  - 右侧显示“当前预算总额”和“异常行数”快速提示

##### 5.3.5.2 附件导入结果页布局

- 页面顶部：
  - 显示导入来源合同名称、附件文件名、上传时间、解析状态
- 解析摘要卡片：
  - 总物资行数、已识别条目数、缺失字段数、异常行数
  - 提示“请补全红色标记字段后保存草稿”
- 主内容区：
  - 左侧为物资预览表，包含“物资名称/规格型号/品牌/单位/计划数量/预算单价/预算合价/备注/解析状态”
  - 右侧为原始附件预览与导入说明：支持查看附件名称、解析规则、常见错误说明
- 行级交互：
  - 解析异常行使用黄色背景，鼠标悬浮显示“解析失败原因”
  - 支持行内编辑、删除、补充字段、添加备注
  - 支持批量选择并“标记为已确认”
- 操作按钮：
  - 保存草稿、继续编辑、取消导入
  - 若存在重大解析异常，按钮旁显示“建议先手工补全后保存草稿”文案

##### 5.3.5.3 校验与反馈机制

- 行级校验状态：
  - 成功：绿色勾选
  - 警告：黄色感叹号（例如无基准价或未填写品牌）
  - 错误：红色叉号（例如计划数量为空）
- 顶部警告条：
  - 显示“共有 X 行存在校验问题，请先修正后提交审批”
  - 点击可跳转至第一个异常行
- 提交审批前校验：
  - 检查必填字段、设备类品牌、计划数量、已确认来源、合同信息完整性
  - 若校验失败，阻止提交并展示详细问题列表
- 反馈提示：
  - 保存草稿成功后显示“保存成功，可继续编辑”Toast
  - 提交审批成功后显示“已提交审批，草稿转为待审批状态”
  - 删除草稿时需二次确认弹窗，并提示"删除后无法恢复"

### 5.4 甘特图管理页

#### 5.4.1 甘特图列表页

- 页面顶部操作区：
  - 按钮：新建甘特图、刷新
  - 仅项目经理可见"新建甘特图"按钮
- 筛选区：
  - 项目、状态（草稿/待审批/已审批/进行中）、创建时间
- 列表字段：
  - 项目名称、甘特图名称、状态、创建人、创建时间、最后更新时间、操作
- 行操作：
  - 查看、编辑（草稿可编辑）、提交审批、删除
- 状态标识：
  - 草稿：灰色标签
  - 待审批：橙色标签
  - 已审批：绿色标签
  - 进行中：蓝色标签

#### 5.4.2 甘特图编辑页

**页面总体布局：**
- 顶部：项目信息区、甘特图名称、页面状态、操作按钮区
- 中部：里程碑和任务节点表单区（支持批量填写）
- 下部：甘特图可视化预览区（时间线展示）

**顶部操作按钮区：**
- 保存草稿按钮：保存当前编辑内容为草稿
- 提交审批按钮：提交里程碑和任务节点结构进行审批（审批通过后结构锁定，进度可继续更新）
- 取消按钮：返回甘特图列表，放弃编辑

**中部：里程碑与任务节点表单**

**5.4.2.1 里程碑批量填写表格**

表格结构如下，支持一次性录入多个里程碑：

| 序号 | 里程碑名称 | 计划完成时间 | 描述 | 操作 |
|-----|----------|-----------|-----|------|
| 1 | 地基验收完成 | 2026-06-30 | 基础工程验收 | 删除 |
| 2 | 主体结构竣工 | 2026-09-15 | 主体钢结构成型 | 删除 |
| + | (新增行) | - | - | - |

**表格操作说明：**
- 每行可编辑里程碑名称（必填）和计划完成时间（必填，日期选择器）
- 描述字段可选填（补充说明信息）
- 支持"添加新里程碑行"按钮，动态增添行数
- 每行右侧有删除按钮，支持删除行
- 里程碑名称支持下拉选择，系统提前定义常用里程碑类型（地基验收、主体结构、装修竣工等）或自由输入

**验证规则：**
- 每个里程碑名称非空、计划完成时间非空
- 计划完成时间应晚于项目开始日期
- 同一甘特图内里程碑名称不重复
- 至少需要一个里程碑

**5.4.2.2 任务节点批量填写表格**

在里程碑表格下方添加任务节点表格，支持多个里程碑下创建多个任务：

| 里程碑 | 任务名称 | 计划开始时间 | 计划完成时间 | 负责人 | 隐蔽工程达标 | 紧前关系 | 操作 |
|------|--------|-----------|-----------|--------|-----------|---------|------|
| 地基验收完成 | 地基开挖 | 2026-05-01 | 2026-05-15 | 张三 | ☐ | - | 删除 |
| 地基验收完成 | 地基检测 | 2026-05-16 | 2026-05-30 | 李四 | ☑ | 地基开挖(FS,+1天) | 删除 |
| 主体结构竣工 | 钢结构吊装 | 2026-06-01 | 2026-08-30 | 王五 | ☑ | 地基检测(FS) | 删除 |
| + | (新增行) | - | - | - | - | - | - |

**表格字段说明：**

1. **里程碑**（下拉选择）：
   - 关联上方里程碑表格中定义的里程碑
   - 支持一个里程碑下有多个任务节点

2. **任务名称**（文本输入，必填）：
   - 描述该任务的具体内容
   - 长度限制 50 字以内

3. **计划开始时间**（日期选择器，必填）：
   - 指任务实际开始执行的日期
   - 应晚于项目开始日期
   - 应不晚于计划完成时间

4. **计划完成时间**（日期选择器，必填）：
   - 指任务预计完成的日期
   - 应晚于计划开始时间
   - 应不晚于所属里程碑的计划完成时间

5. **负责人**（下拉选择，必填）：
   - 从通讯录/部门成员中选择
   - 默认为当前项目经理
   - 支持输入关键词快速搜索

6. **隐蔽工程达标**（复选框，可选）：
   - ☐ 未勾选：该任务节点无隐蔽工程要求
   - ☑ 已勾选：该任务节点有隐蔽工程，需要隐蔽工程检验达标
   - 勾选后，在项目进度填报时需要补充隐蔽工程验收证明、检查记录等附件

7. **紧前关系**（关系配置，可选）：
   - 指定该任务的直接前置任务及其关系类型
   - 支持配置多个紧前任务（若多个任务均作为紧前），通过"+添加紧前"按钮增加关系条目
   
   **紧前关系配置子表结构：**
   
   | 紧前任务 | 关系类型 | 滞后量（天） |
   |---------|--------|----------|
   | 地基开挖 | FS | +1 |
   | 钢筋验收 | FS | 0 |
   
   **配置说明：**
   - **紧前任务**（下拉选择，必填）：
     - 列出该甘特图中所有已定义的任务节点
     - 支持搜索快速定位
     - 选择后，系统自动检测循环依赖，若发现循环则提示错误
   
   - **关系类型**（下拉选择，必填）：
     - **FS（完成-开始）**：紧前任务完成后，本任务才能开始（最常用）
     - **SS（开始-开始）**：紧前任务开始后，本任务才能开始（并行开始）
     - **FF（完成-完成）**：紧前任务完成后，本任务才能完成（并行完成）
     - **SF（开始-完成）**：紧前任务开始后，本任务才能完成（少用）
   
   - **滞后量**（数值输入，可选，支持负数）：
     - 表示关系类型中的时间间隔（单位：天）
     - **正数**：延后效应（如：FS,+2表示紧前任务完成后延后2天开始）
     - **负数**：提前效应（如：FS,-1表示紧前任务完成前1天即可开始，通常用于重叠作业）
     - **0**：无间隔（默认值，不填时系统自动置为0）
   
   - **操作按钮**：
     - 删除该紧前关系配置
     - "+添加紧前"：在该任务下继续添加其他紧前任务
   
   **示例解释：**
   - 地基开挖 → 地基检测(FS,+1天)：地基开挖完成后，延后1天（预留养护时间）才能开始地基检测
   - 地基检测 → 钢结构吊装(FS,0)：地基检测完成后，立即可开始钢结构吊装
   - 模板搭建 → 混凝土浇筑(SS,+2天)：模板搭建开始后，延后2天可开始混凝土浇筑

8. **操作**（按钮）：
   - 删除行按钮，删除前二次确认

**表格操作说明：**
- 支持"添加新任务"按钮，动态增添行数
- 允许拖拽调整行顺序
- 表格左侧可多选行，实现批量删除或批量设置操作（如批量修改负责人、批量勾选隐蔽工程）
- 支持导入 Excel 功能：用户可在 Excel 中编辑任务清单后导入系统，系统自动解析字段

**验证规则：**
- 每个任务的计划开始时间、计划完成时间、负责人非空
- 计划开始时间 ≤ 计划完成时间
- 计划完成时间 ≤ 所属里程碑的计划完成时间
- 计划开始时间 ≥ 项目的计划开始日期
- 紧前关系不能形成循环依赖：系统自动检测并阻止
- 紧前任务必须是同一甘特图内已定义的任务
- 关系类型必须选择：FS/SS/FF/SF 其中之一
- 滞后量为0或正负数值，不允许文本
- 若任务有紧前关系，系统自动推算合理的计划开始时间，用户可选择接受或手动调整
- 同一里程碑内任务名称不重复
- 至少需要一个任务节点

**下部：甘特图可视化预览**

在表单下方实时展示甘特图可视化效果，用户可在此预览编辑结果：

**甘特图可视化组件：**
- 时间轴：支持日/周/月视图切换
- 里程碑显示：上方显示里程碑标签，用红色菱形◇标记计划完成时间点
- 任务条：
  - 蓝色实心条表示任务，长度表示工期
  - 任务名称显示在条形上方
  - 任务负责人显示在条形下方（缩写或头像）
  - 隐蔽工程任务加橙色边框标识
- 紧前关系箭头：
  - 虚线箭头连接紧前任务到后置任务，箭头线上标注关系类型和滞后量
  - 不同关系类型箭头样式区分：
    - **FS（完成-开始）**：实线箭头→
    - **SS（开始-开始）**：虚线箭头⇢
    - **FF（完成-完成）**：双向箭头⇄
    - **SF（开始-完成）**：点线箭头⋯→
  - 箭头标签显示格式："关系类型,滞后量"，如"FS,+1d"表示完成后延后1天开始，"SS,0"表示同时开始
  - 鼠标悬停箭头显示详细的紧前关系说明
- 平行任务：多个任务并行显示在不同行
- 进度条：审批后任务条内绿色填充表示完成进度（编辑阶段不显示）
- 预警标识：逾期任务红色边框，临近逾期橙色边框

**预览交互：**
- 鼠标悬停任务条显示任务详情 Tooltip（名称、计划时间、负责人、隐蔽工程状态、紧前关系列表）
- 点击任务条可高亮该任务及其相关紧前关系（前向：本任务作为后置任务的紧前任务；后向：本任务的紧前任务）
- 支持缩放功能（+/-按钮或鼠标滚轮）调整时间轴粒度
- 右键菜单可快速编辑或删除任务（跳转至表格相应行）

**5.4.2.3 表单校验与提交**

**保存草稿逻辑：**
- 允许表单未完全填写，保存为草稿状态
- 草稿可随时重新编辑
- 显示草稿最后修改时间

**提交审批逻辑：**
- 检查所有必填字段是否完整
- 验证时间逻辑和紧前关系是否正确
- 检查是否至少存在一个里程碑和一个任务
- 校验通过后触发审批流程：项目经理提交 → 总经理审批
- 审批通过后甘特图结构锁定（里程碑和任务节点不可修改），但进度比例和实际日期可继续更新
- 显示校验错误列表，允许用户快速定位修改

**错误提示示例：**
```
✖ 第3行任务"钢结构吊装"的计划开始时间早于所属里程碑"地基验收完成"
✖ 第2行任务"地基检测"的紧前关系形成循环依赖
✖ 第4行任务"地基检测"关系类型必须选择（FS/SS/FF/SF）
✖ 计划完成时间不能为空（第5行）
```

**5.4.2.4 进度填报功能（审批通过后）**

甘特图结构审批通过后，项目经理可进行进度填报。填报方式支持两种：

**方式一：在甘特图中按任务填报**
- 在可视化甘特图区，点击任务条，弹出进度填报面板
- 填报表单包含：
  - 当前进度百分比（0-100%，滑块或数值输入）
  - 进度描述（必填，多行文本，记录该阶段工作内容）
  - 实际开始日期（可选，允许与计划开始日期不同）
  - 实际完成日期（可选，预测或已完成）
  - 附件上传（照片、文档、视频等进度佐证）

**方式二：批量填报**
- 支持多选任务，一次性更新同一进度百分比和统一描述
- 用于多个快速推进的任务同时填报场景

**隐蔽工程验收特殊处理：**
- 若该任务节点被标记为"隐蔽工程达标"（在编辑页勾选），进度填报时：
  - 进度表单自动新增"隐蔽工程验收状态"字段（必填）
  - 选项：未验收 / 验收不合格 / 验收合格
  - 验收合格后方可继续填报后续任务进度（系统校验前置任务状态）
  - 若选择"验收不合格"，必填补充"整改说明"文本和附件（如整改清单、现场照片）
  - 隐蔽工程验收记录作为里程碑验收的重要凭证，整理汇总至竣工资料

**进度填报历史：**
- 每个任务显示完整进度更新历史（时间倒序）
- 历史记录包含：填报时间、填报人、进度值、进度描述、隐蔽工程状态（如有）
- 历史记录不可修改或删除，仅可查看，实现完整溯源

**进度填报后系统计算：**
- 自动计算任务进度百分比
- 自动更新甘特图可视化中的任务条进度填充
- 累计计算里程碑整体完成率（该里程碑下所有任务的平均进度）
- 累计计算项目总体进度
- 触发进度预警规则：若超过计划完成时间仍未完成，标记为逾期

#### 5.4.3 甘特图审批页

- 审批流程：项目经理提交 → 总经理审批
- 审批内容呈现：
  - 甘特图整体预览（只读，可视化展示）
  - 里程碑列表（表格展示：名称、计划完成时间）
  - 任务节点列表（表格展示：里程碑、任务名称、计划时间、负责人、隐蔽工程标记、紧前关系）
  - 紧前关系图（可视化显示任务链条及关系类型、滞后量）
  - 审批意见输入框（必填）
- 审批动作：同意 / 驳回
- 同意动作：
  - 甘特图结构锁定，里程碑和任务节点不可修改
  - 系统自动将任务节点信息及紧前关系同步至收入合同拆分表
  - 状态变更为"已审批"
  - 推送通知给项目经理
  
- 驳回处理：
  - 退回项目经理修改，保留审批意见
  - 项目经理可修改甘特图结构和紧前关系后重新提交
  - 状态回到"草稿"

#### 5.4.4 进度监控页

- 实时进度展示：
  - 甘特图当前状态（可视化展示里程碑和任务进度）
  - 隐蔽工程验收标记（已验收的任务打绿色对号✓，未验收打黄色△，验收不合格打红色✗）
  - 逾期任务高亮显示（红色边框）
  - 临近逾期任务橙色提醒（距计划完成日期≤3天）
  
- 进度统计：
  - 总体完成率（各任务进度加权平均）
  - 里程碑达成情况（每个里程碑的完成率）
  - 隐蔽工程验收统计（已验收/未验收/验收不合格数量）
  - 任务延期统计（逾期任务数、最长延期天数）
  
- 预警信息：
  - 逾期任务列表（任务名、逾期天数、负责人）
  - 隐蔽工程待验收提醒（需要立即补充隐蔽工程验收的任务）
  - 即将逾期任务提醒（距完成时间≤3天）
  - 关键路径分析（显示最长链条的任务序列）

#### 5.4.5 进度报告页

- 自动生成：基于甘特图数据自动生成项目进度报告
- 报告内容：
  - 项目基本信息
  - 里程碑完成情况
  - 任务进度汇总
  - 延期任务分析
  - 下一步工作计划
- 导出功能：支持PDF导出，带公司水印

### 5.6 付款计划页

#### 5.4.1 计划表管理页

- 目标：支出合同付款计划表仅系统内查阅、编辑，不可导出。
- 页面顶部说明文本："该页面内容仅供系统内部查看，禁止导出与打印。"
- 表格字段：合同编号、条款编号、计划付款日期、计划金额、节点说明、备注
- 操作：编辑、保存、提交审批
- 禁止：导出按钮隐藏，打印按钮隐藏

#### 5.4.2 月度付款计划页

- 筛选项：月份、项目、合同类型
- 统计卡片：材料类、设备类、人工类已付/应付汇总
- 表格字段：
  - 合同、计划金额、已付金额、本次应付、状态
- 操作：查看、生成月度计划、导出
- 详情页：
  - 支出计划摘要、历史已付明细、当月应付明细
- 说明标签："支出合同付款计划表不允许导出"、"月度付款计划支持导出"。

### 5.6 日常报销页

#### 5.5.1 报销单列表页

- 筛选项：部门、项目、合同、费用类型、状态、日期
- 统计卡片：待审批数、已支付数、预算预警项
- 表格字段：
  - 报销单编号、报销人、部门、金额、预算占用%、状态、审批状态
- 行操作：查看、编辑、提交、导出（仅报销单）

#### 5.5.2 报销单编辑页

- 页面分区：
  - 基础信息区：报销人、部门、项目、合同、费用类型
  - 明细区：多笔费用明细可增删
  - 附件区：拖拽上传、在线预览
  - 预算信息区：当前预算、已用、剩余、占用比例
- 规则提示：
  - 超预算警告、招待费限额、时效校验
- 提交按钮旁提示：
  - "预算占用≥80%预警；≥100%将禁止提交"

#### 5.5.3 报销单详情页

- 展示完整信息：单据、明细、附件、审批轨迹、预算明细
- 异常状态突出：超期、超限、发票未核验等使用红色标签
- 按钮区：重新提交、导出、审批记录查询

### 5.7 发票管理页

- 列表页：发票编号、类型、金额、开票日期、关联单据、认证状态、过期预警
- 详情页：发票信息、关联记录、附件、预警倒计时
- 交互：
  - 发票认证状态支持手动刷新
  - 过期预警条展示“距认证到期还有X天”

### 5.8 询价录入与产品价格双库页

#### 5.8.1 询价录入页

- 字段区：产品名称、规格型号、品牌、单价、税率、备货周期、预计到货时间、供应商名称、询价日期、录入人、备注。
- 校验规则：
  - 单价/税率/备货周期必须为正数；
  - 预计到货时间必须晚于询价日期；
  - 核心字段缺失时禁止保存。
- 操作区：保存草稿、提交入库、批量Excel导入、下载模板。
- 批量导入结果弹窗：成功条数/失败条数/失败原因明细（支持导出错误行）。

#### 5.8.2 产品价格双库管理页

- 双标签页：`产品价格基准数据库` / `产品价格基础数据库`。
- 基准库列表字段：物资名称、规格型号、品牌、物资类型、基准价、税率、生效状态、更新时间、update_type、操作。
- 基础库列表字段：来源（询价/合同）、价格、税率、供应商、备货周期、预计到货、purchase_time、关联contract_id。
- 交互：支持按物资类型（材料/设备/劳务）筛选、查看历史轨迹、触发手动重算。

### 5.9 采购清单价格匹配与比价页（增强）

#### 5.9.1 二级价格匹配反馈

- 采购清单编辑页在每行显示匹配来源标签：
  - 绿色：基准库命中；
  - 蓝色：基础库命中（最近记录）；
  - 灰色：价格待询价。
- 行内展开“匹配详情”：展示命中规则（名称/规格/品牌）、匹配时间、来源记录ID。
- 对“价格待询价”行显示快捷入口：`发起询价`、`临时填价（需审批）`。

#### 5.9.2 自动比价分析弹窗

- 入口：采购清单行操作“自动比价”。
- 对比维度：不含税价、含税价、备货周期、预计到货时间、综合性价比分。
- 排序切换：最低含税价 / 最快到货 / 综合性价比。
- 一键生成《产品采购比价分析报告》（PDF/Excel）。

#### 5.9.3 零星采购1.5%超限额提示

- 提交零星采购时显示计算卡片：历史累计、本次金额、批量采购总额、1.5%阈值、是否超限。
- 超限额时强提示并锁定审批流文案：
  - “本次零星采购累计金额已超项目批量采购总额的1.5%，已自动升级为总经理审批流程”。
- 审批流预览组件只读展示：采购员 → 预算员 → 总经理。

### 5.10 供应商价格水平等级页

- 分类Tab：设备 / 材料 / 劳务（分榜展示，不混排）。
- 列表字段：供应商、供应数量、供应金额、综合排名、等级（优于行业/普通/较差）、最近重算时间。
- 详情侧栏：取数来源（基准库/基础库）、计算权重（50%数量+50%金额）、异常数据过滤说明。
- 操作：手动重算、查看重算日志、导出分类评级结果。
- 异常状态提示：
  - 数据源连接异常：显示重试计数和“稍后重算”按钮；
  - 空数据分类：显示“当前分类无有效数据，已跳过计算”。

### 5.11 物资出库与跨项目调拨管控页

- 出库单表单新增必填关联区：项目合同编号 / 入库单号（二选一必填）。
- 未填关联字段时：
  - 保存按钮禁用；
  - 字段下红色提示“现场出库必须关联项目合同编号或入库单号”。
- 跨项目调拨拦截弹窗（强阻断）：
  - 文案：“当前操作涉及物资跨项目调拨，未完成调拨单审批流程，禁止出库”；
  - 按钮：`去创建调拨单`、`取消`。

### 5.12 甘特图与收入拆分一体化页（增强）

- 甘特编辑页右侧新增“拆分映射状态面板”：
  - 里程碑/任务节点是否已映射；
  - 子任务新增后待分配金额提醒；
  - 审批锁定状态（已锁定/待重新审批）。
- 子任务新增交互：
  - 仅项目经理可见“新增子任务”；
  - 锁定状态下按钮灰置并提示“需撤回审批后编辑结构”。
- 联动校验提示：
  - 任务金额闭环校验（总和=收入合同含税金额，容差0.01）；
  - 时间逻辑校验（计划开始≤计划完成）；
  - 映射一致性异常即时弹窗阻断。
- 拆分审批与甘特审批状态条统一展示，便于识别“已通过但待重提”的场景。

---

## 7. 交互细节与规范

### 7.1 按钮与权限

- 操作按钮按权限显示。
- 无权限按钮不可见或禁用。
- 关键操作（提交、审批、删除、转办）必须确认弹窗。
- 表单提交后禁止重复点击。

### 7.2 颜色与状态指示

- 正常：#2E8B57（绿色）
- 预警：#FFA500（橙色）
- 逾期/异常：#DC3545（红色）
- 信息提示：#0D6EFD（蓝色）
- 禁用/不可选：#ADB5BD（灰色）

### 7.3 字体与排版

- 标题：16-18px，加粗
- 正文字体：14px
- 关键数据数字：18px+加粗
- 表格行高：22px

### 7.4 表单校验

- 即时校验：必填项、金额范围、日期范围、附件格式
- 错误反馈：字段下方红色提示文案
- 提交前整体校验：若存在异常，则禁止提交并高亮错误区域

### 7.5 附件上传体验

- 拖拽上传与点击上传二合一
- 支持批量上传
- 显示状态：上传中、已完成、失败
- 支持预览：PDF、Office、图片；压缩包显示文件列表
- 删除操作需二次确认

### 7.6 审批流程体验

- 审批记录时间线按时间倒序
- 驳回原因展示在首位
- 审批动作按钮放在页面右上角或底部固定区域
- 审批意见为必填时，新增输入区域

---

## 8. 样式建议

### 8.1 主题

- 企业级浅色主题，辅以深蓝+灰白倍显专业
- 关键状态使用高对比色，避免冲突

### 8.2 卡片与容器

- 卡片圆角8px
- 阴影：0 2px 8px rgba(0,0,0,0.08)
- 内边距：24px

### 8.3 表格风格

- 表头背景：#F8F9FA
- 表体行高：50px
- 悬停高亮：#F1F3F5
- 分页居右显示

### 8.4 图表风格

- 颜色方案：蓝、橙、绿、红、灰
- 除了金额图表，还需带百分比标签
- 图表支持导出截图

---

## 9. 移动/平板适配说明

- 阻止复杂数据页面在小屏直接使用，仅保留关键列表与详情。
- 报表页和对账页面优先提供平板横屏体验。
- 移动端仅展示待办、报表摘要、审批操作、提醒消息。

---

## 9. 交付物

- 页面原型设计稿
- 组件/页面规范文档
- 交互流程图
- 状态与错误提示规范
- 样式变量表
- 权限控制展示方案

---

## 10. 备注

本UI设计说明基于 MOCHU-OA V3.0 综合需求（含完整合并版第3A章、第3C章）整理，覆盖财务、采购、供应商、物资、施工等关键模块。建议由产品与设计团队进一步细化原型图、视觉稿和前端组件库实现。